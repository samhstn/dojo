# Lessons

A place to record learnings.

Each lesson is stored in the following format:

```bash
<lesson_name>.<ext>
```

Usually this is one file (for elixir we store both an `.ex` and an `.exs` file).
